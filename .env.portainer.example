# Portainer Configuration Example
# Copy this file to .env and update with your actual values

# Portainer Server Configuration
PORTAINER_URL=http://192.168.1.100:9000
# or for HTTPS: https://portainer.yourdomain.com

# Authentication Options (in order of preference):

# Option 1: API Access Token (RECOMMENDED - Never expires)
# To get an access token:
# 1. Login to Portainer web UI
# 2. Click your username → My Account
# 3. Scroll to "Access tokens" section
# 4. Click "Add access token"
# 5. Provide description and your password
# 6. Copy the token (shown only once!)
PORTAINER_ACCESS_TOKEN=ptr_YOUR_ACCESS_TOKEN_HERE

# Option 2: JWT Token (Valid for 8 hours, if you already have one)
# PORTAINER_JWT_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Option 3: Username/Password (Will generate 8-hour JWT token)
# Not recommended for production use
# PORTAINER_USERNAME=admin
# PORTAINER_PASSWORD=your-secure-password-here

# Optional: Default endpoint ID (usually 1 for local Docker)
# PORTAINER_ENDPOINT_ID=1

# Optional: Docker Registry Configuration (for pushing built images)
# DOCKER_REGISTRY_URL=registry.yourdomain.com:5000
# DOCKER_REGISTRY_USERNAME=registry-user
# DOCKER_REGISTRY_PASSWORD=registry-password

# Optional: Default Stack Environment Variables
# These will be available to all deployed stacks
# STACK_ENV_DATABASE_URL=postgres://user:pass@db:5432/myapp
# STACK_ENV_REDIS_URL=redis://redis:6379